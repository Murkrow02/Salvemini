<?xml version="1.0" encoding="UTF-8"?>
<ContentPage Title="iCringe" xmlns:local="clr-namespace:SalveminiApp" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="SalveminiApp.AreaVip.ApprovaCringe">
    <ListView  Refreshing="domande_Refreshing" ItemSelected="domanda_Selected" IsPullToRefreshEnabled="True" BackgroundColor="Transparent" x:Name="domandeList" HasUnevenRows="true" Header="" Footer="" SeparatorVisibility="Default">
        <!--Header-->
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Frame HasShadow="False" Margin="10" Padding="5">
                        <StackLayout>
                            <Label Text="{Binding Domanda}" FontSize="15" TextColor="{x:Static local:Styles.TextColor}"/>
                        </StackLayout>
                    </Frame>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
