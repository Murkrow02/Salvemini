<?xml version="1.0" encoding="UTF-8"?>
<ContentPage Title="Argo" BackgroundColor="{x:Static local:Styles.BGColor}" xmlns:local="clr-namespace:SalveminiApp" xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms" xmlns:fftransformations="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Transformations" xmlns:ffimageloadingsvg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms" xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="SalveminiApp.ArgoPage">
    <StackLayout x:Name="mainLayout" Padding="0, 20">
         <CollectionView VerticalOptions="Start" ItemsLayout="HorizontalList" x:Name="widgetCollection" Margin="-15,0" HorizontalScrollBarVisibility="Never">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <local:ArgoWidget Margin="30,0" Title="{Binding Title}" Icon="{Binding Icon}" StartColor="{Binding StartColor}" EndColor="{Binding EndColor}" Push="{Binding Push}">
                            <local:ArgoWidget.GestureRecognizers>
                                <TapGestureRecognizer NumberOfTapsRequired="1" Tapped="Widget_Tapped" />
                            </local:ArgoWidget.GestureRecognizers>
                        </local:ArgoWidget>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
    </StackLayout>
</ContentPage>