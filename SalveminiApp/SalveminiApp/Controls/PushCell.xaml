<?xml version="1.0" encoding="UTF-8"?>
<ContentView VerticalOptions="Start" BackgroundColor="Gray" xmlns:local="clr-namespace:SalveminiApp" xmlns:iconize="clr-namespace:Plugin.Iconize;assembly=Plugin.Iconize" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="SalveminiApp.Helpers.PushCell">
    <ContentView.Content>
        <StackLayout x:Name="cell" BackgroundColor="White" VerticalOptions="Start"  Padding="10,10" Spacing="8">
            <StackLayout Orientation="Horizontal">
                <Label x:Name="TitleLbl" TextColor="{x:Static local:Styles.TextColor}" HorizontalOptions="StartAndExpand" HorizontalTextAlignment="Center" />
                <iconize:IconLabel x:Name="arrow" Text="fas-chevron-right" TextColor="{x:Static local:Styles.TextGray}" HorizontalOptions="EndAndExpand" HorizontalTextAlignment="Center" />
                <ActivityIndicator x:Name="loading"  HorizontalOptions="EndAndExpand" IsVisible="false" />
            </StackLayout>
            <Frame Margin="0,0,-10,-10" x:Name="separator" HasShadow="False" BackgroundColor="{x:Static local:Styles.ObjectGray}" HeightRequest="1" Padding="0" VerticalOptions="EndAndExpand"/>
        </StackLayout>
    </ContentView.Content>
    <ContentView.GestureRecognizers>
        <TapGestureRecognizer Tapped="animateBg" NumberOfTapsRequired="1"/>
    </ContentView.GestureRecognizers>
</ContentView>
