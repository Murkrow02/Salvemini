<?xml version="1.0" encoding="UTF-8"?>
<ContentView VerticalOptions="Start" BackgroundColor="Gray" x:Name="cell" xmlns:local="clr-namespace:SalveminiApp" xmlns:iconize="clr-namespace:Plugin.Iconize;assembly=Plugin.Iconize" xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="SalveminiApp.Helpers.PushCell">
    <ContentView.Content>
        <StackLayout x:Name="cell" BackgroundColor="White" VerticalOptions="Start"  Padding="10,10" Spacing="8">
        <StackLayout Orientation="Horizontal">
                <Label x:Name="TitleLbl" Text="asd" TextColor="{x:Static local:Styles.TextColor}" HorizontalOptions="StartAndExpand" HorizontalTextAlignment="Center" />
            <iconize:IconLabel Text="fas-chevron-right" TextColor="{x:Static local:Styles.TextGray}" HorizontalOptions="EndAndExpand" HorizontalTextAlignment="Center" />
        </StackLayout>
        <Frame x:Name="separator" HasShadow="False" BackgroundColor="{x:Static local:Styles.ObjectGray}" HeightRequest="1" Padding="0" />
            </StackLayout>

    </ContentView.Content>
    <ContentView.GestureRecognizers>
        <TapGestureRecognizer Tapped="animateBg" NumberOfTapsRequired="1"/>
    </ContentView.GestureRecognizers>
</ContentView>
